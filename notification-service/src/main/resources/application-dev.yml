spring:
  datasource:
    url: jdbc:h2:mem:notification-service
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true