spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:media-service
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true